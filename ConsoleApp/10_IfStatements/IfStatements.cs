using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;

namespace IfStatements
{
    /*
    public class IfStatements
    {
        static void Main(string[] args)
        {
            /*
            System.Console.WriteLine("Enter your name: ");
            string name = Console.ReadLine();

            System.Console.Write("Enter your age: ");
            string ageInput = Console.ReadLine();
            int age = Convert.ToInt32(ageInput);

            System.Console.WriteLine();
            System.Console.WriteLine("Your name is " + name + " and your age is " + age);
            */

            // ==, >, >=, <, <=, !=, ||, &&

            /*
            if (age >= 18 && age <= 25) // 18 > 18
            {
                System.Console.WriteLine("You are between 18 and 25.");
            }
            else if (age >= 26)
            {
                System.Console.WriteLine("You are 26 or older.");
            }
            */

            /*
            if (age < 0 || age > 150)
            {
                System.Console.WriteLine("Invalid age!");
            }
            else
            {
                if (age >= 18 && age <= 25)
                {
                    System.Console.WriteLine("You are between 18 and 25.");
                }
                else if (age >= 26)
                {
                    System.Console.WriteLine("You are 26 or older.");
                }
            }
            */

            /*
            System.Console.Write("Enter the first number: ");
            string numberAInput = Console.ReadLine();
            int numberA = Convert.ToInt32(numberAInput);
    
            System.Console.Write("Enter the second number: ");
            string numberBInput = Console.ReadLine();
            int numberB = Convert.ToInt32(numberBInput);

            int answer = numberA * numberB;

            System.Console.Write("Value of " + numberA + " x " + numberB + ": ");
            string answerInput = Console.ReadLine();
            int actualAnswer = Convert.ToInt32(answerInput);

            if (answer == actualAnswer)
            {
                System.Console.WriteLine("Well Done!");
            }
            else
            {
                System.Console.WriteLine("Close but it was wrong.");
            }
            
            Console.ReadLine();
        }
    }
    */
}