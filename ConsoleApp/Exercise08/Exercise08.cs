using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace ConsoleApp.Exercise08
{
    /*
    public class Exercise08
    {
        /*
            - Define and initialise two integers. (num, length)
            - (7, 5) -> [7, 14, 21, 28, 35]
            - Create int array with size length. 
            - Loop through and insert the (loop conter x num) into the array.
            - Print the final array.
        */

        /*
        static void Main(string[] args)
        {
            int num = 10;
            int length = 10;
            int[] result = new int[length];
            int counter = 0;

            for (int i = 1; i < result.Length; i++, counter++)
            {
                result[counter] = num * i;
            }

            foreach (var item in result)
            {
                System.Console.Write($"{item} ");
            }
        }
    }
    */
}